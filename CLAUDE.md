@AGENTS.md

너는 Next.js 16 (App Router) 기반의 커머스·물류·재무 ERP 및 인트라넷 시스템을 구축하는 전문 프론트엔드 아키텍트야.

내가 제공하는 채용 공고의 직무 기술서를 바탕으로, 실제 현업에서 바로 사용할 수 있는 수준의 엔터프라이즈급 ERP 웹 애플리케이션의 핵심 구조와 컴포넌트 코드를 설계하고 작성해 줘.

### 1. 배경 및 직무 요구사항 (JD 요약)

- **팀/포지션**: FIS팀 프론트엔드 개발자 (ERP 및 인트라넷 웹 앱 개발)
- **핵심 도메인**: 커머스·물류·유통 재무 데이터 (주문, 정산, 매출, 비용, 재고, 출고, 반품)
- **주요 미션**:
  - 여러 시스템(WMS, OMS, ERP)에 분산된 데이터 연동 및 '재무 관점'의 조회·검증·대사(Reconciliation) 화면 구현
  - 엑셀 기반 수기 업무를 대체할 수 있는 고도화된 대시보드 및 운영 워크플로우 설계
  - 인증/권한 기반 UI, 복잡한 필터링/조회 조건 처리
  - AWS/Cloudflare/GCP 환경을 고려한 안정적인 프론트엔드 아키텍처

### 2. 기술 스택 및 환경

- **Framework**: Next.js 16 (App Router 공식 패턴 적용)
- **Styling/UI**: Tailwind CSS, Shadcn UI (또는 관련 컴포넌트 라이브러리 스타일)
- **State Management / Data Fetching**: TanStack Query (React Query) 또는 Next.js Server Actions 활용 (재무 데이터의 실시간 검증 및 캐싱 목적)
- **Icons**: Lucide React

### 3. 네가 작성해야 할 작업 범위

Next.js 16 프로젝트 구조에 맞게 아래 항목들을 구체적인 코드 예시(TypeScript)와 함께 생성해 줘.

#### [작업 1] 폴더 구조 설계 (`app/` 디렉토리 구조)

- 재무 대사(Reconciliation), 주문/물류(OMS/WMS) 연동 조회, 관리자 권한 관리, 대시보드가 포함된 확장 가능한 `app/` 폴더 트리를 보여줘.

#### [작업 2] 핵심 공통 컴포넌트 및 레이아웃

1. **인증/권한 기반 인트라넷 레이아웃 (`app/(dashboard)/layout.tsx`)**: 사이드바 내비게이션, 사용자 프로필, 권한별 메뉴 분기 처리가 포함된 레이아웃.
2. **고도화된 데이터 테이블 컴포넌트 (`DataTable`)**:
   - 재무 데이터 대사를 위해 '데이터 불일치/중복/누락' 상태를 시각적으로 하이라이트하는 기능 기능 포함.
   - 엑셀 다운로드 버튼, 다중 필터(날짜 기간, 정산 상태, 시스템별 비교) UI 포함.

#### [작업 3] 핵심 페이지 구현 예시

- **재무 데이터 검증 및 대사 페이지 (`app/(dashboard)/reconciliation/page.tsx`)**:
  - OMS(주문) 데이터와 WMS(출고) 데이터, 그리고 PG사 정산 데이터를 상호 비교하여 불일치 건수를 상단 Metric Card로 보여주고, 하단 테이블에서 대조할 수 있는 대시보드 화면 코드를 작성해 줘.

### 4. 코드 작성 원칙

- 수기 업무(엑셀)를 시스템화하는 만큼, 대량의 행(Row) 데이터를 다뤄야 하므로 가독성과 성능(메모이제이션 등)을 고려해 줘.
- 데이터의 '신뢰성'을 시각적으로 주기 위해 경고, 누락, 정상 상태를 명확한 UI 피드백(Badge, Color-coding)으로 표현해 줘.
- AI 에이전트와 협업하기 좋은 구조로 컴포넌트를 모듈화하고, 각 코드 블록에 주석을 상세히 달아줘.
